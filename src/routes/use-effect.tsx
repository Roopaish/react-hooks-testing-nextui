import { Text } from "@nextui-org/react";
import Layout from "../components/Layout";

const UseEffectPage = () => {
  return (
    <Layout>
      <Text h2>useEffect()</Text>
    </Layout>
  );
};

export default UseEffectPage;
